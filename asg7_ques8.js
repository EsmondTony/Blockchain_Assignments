// Write a Javascript function to test whether the first character of a string
// is lowercase.

str = prompt("Enter a string")
if(str[0] == str[0].toLowerCase()){
    console.log("The first character of a string is in lower case")
}else{
    console.log("The first character of a string is in upper case")
}